import {getChatGPTUser,chatGPTSignInPath} from '@/app/chatgpt-auth';
import {isAdmin,runtime,cashfreeConfig} from '@/lib/shop';
import Studio from './studio';
import {onlineReady} from '@/lib/payments';
export const dynamic='force-dynamic';
export default async function Page(){const user=await getChatGPTUser();return <><header className="header"><a className="brand" href="/">artelier<span>✳</span></a><a href="/">← Back to the shop</a></header>{await isAdmin()?<Studio onlineReady={onlineReady()||cashfreeConfig().enabled}/>:<main className="studio"><span className="eyebrow">STORE ADMINISTRATION</span><h1>Owner administration.</h1><p>Manage seller approvals, listings and store operations.</p><div className="studio-gate"><h2>{runtime().ADMIN_EMAIL?'Owner sign-in required':'Let’s connect your studio.'}</h2><p>{runtime().ADMIN_EMAIL?'The studio is reserved for the shop owner. Please sign in with the approved account.':'Your studio is built and protected. Share the email address you use for ChatGPT with your site builder to enable owner access.'}</p>{user?<p>Signed in as <strong>{user.email}</strong></p>:<a href={chatGPTSignInPath('/studio')} target="_top">Sign in with ChatGPT →</a>}<p>Want to sell your art? <a href="/sell">Apply through the seller portal.</a></p></div></main>}</>}
