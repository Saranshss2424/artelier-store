import {isAdmin,runtime} from '@/lib/shop';
export async function GET(req:Request){const id=new URL(req.url).searchParams.get('id');if(!id||!/^[a-zA-Z0-9-]+$/.test(id))return new Response('Not found',{status:404});const o=await runtime().ART.get(id);if(!o)return new Response('Not found',{status:404});return new Response(o.body,{headers:{'Content-Type':o.httpMetadata?.contentType||'image/jpeg','Cache-Control':'public,max-age=31536000,immutable','X-Content-Type-Options':'nosniff'}});}
export async function POST(req:Request){if(req.headers.get('origin')!==new URL(req.url).origin||!await isAdmin())return Response.json({error:'Owner access required'},{status:403});try{if(Number(req.headers.get('content-length')||0)>5200000)throw Error();const f=(await req.formData()).get('file') as File;if(!f||f.size>5000000||!['image/jpeg','image/png','image/webp'].includes(f.type))throw Error();const bytes=new Uint8Array(await f.arrayBuffer());const valid=f.type==='image/jpeg'?bytes[0]===255&&bytes[1]===216:f.type==='image/png'?bytes[0]===137&&bytes[1]===80: new TextDecoder().decode(bytes.slice(8,12))==='WEBP';if(!valid)throw Error();const id=crypto.randomUUID();await runtime().ART.put(id,bytes,{httpMetadata:{contentType:f.type}});return Response.json({image:'/api/image?id='+id});}catch{return Response.json({error:'Upload a JPG, PNG or WebP smaller than 5 MB.'},{status:400});}}
